[pytest]
addopts =
    -m "not network"
    --strict-markers
filterwarnings =
markers =
    gpu: tests requiring a GPU
    cpu: tests that run on CPU only
    network: tests that require network access
