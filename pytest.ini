[pytest]
addopts =
    -m "not network"
    --strict-markers
filterwarnings =
    ignore:Could not find a config file in.*:UserWarning:peft.utils.save_and_load
markers =
    gpu: tests requiring a GPU
    cpu: tests that run on CPU only
    network: tests that require network access
