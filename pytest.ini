[pytest]
addopts =
    -m "not network"
    --strict-markers
filterwarnings =
markers =
    gpu: tests requiring a GPU
    cpu: tests that run on CPU only
    network: tests that require network access
    finetune: tests requiring PEFT/LoRA stacks and a GPU environment (opt-in)
