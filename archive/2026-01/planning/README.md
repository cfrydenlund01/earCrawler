# Archived planning artifacts (2026-01)

These files were moved out of the repository root during repo hygiene work.

- `prompt.txt`, `next_prompt.txt`: internal prompt scaffolding (not referenced by the CLI, CI, or docs).
- `Short Roadmap.docx`: internal roadmap referenced only from the archived prompt.
- `title-15.json`: unreferenced dataset snapshot kept for possible future use.

